<div class="mx-auto max-w-3xl space-y-6">
  <div>
    <%= link_to "← Voltar para o gabarito", prova_gabarito_path(@prova), class: "text-sm font-semibold text-slate-700 hover:text-slate-900" %>
  </div>

  <div>
    <h1 class="text-2xl font-bold tracking-tight text-slate-900">Preencher gabarito</h1>
    <p class="mt-1 text-slate-600"><%= @prova.titulo %></p>
  </div>

  <%= render "form", prova: @prova, questao: @questao, gabarito: @gabarito %>
</div>
