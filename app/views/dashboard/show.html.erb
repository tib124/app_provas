<div class="relative isolate min-h-screen bg-gradient-to-br from-slate-50 via-white to-slate-100">
  <!-- Background decorative elements -->
  <div class="pointer-events-none absolute inset-0 -z-10">
    <div class="absolute -left-40 -top-32 h-96 w-96 rounded-full bg-indigo-100/40 blur-3xl"></div>
    <div class="absolute -right-40 top-1/2 h-96 w-96 rounded-full bg-emerald-100/40 blur-3xl"></div>
    <div class="absolute left-1/2 bottom-0 h-96 w-96 rounded-full bg-violet-100/30 blur-3xl"></div>
  </div>

  <div class="relative px-4 py-12 sm:px-6 lg:px-8">
    <!-- Header Section -->
    <div class="mb-12 max-w-7xl mx-auto">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-indigo-50 border border-indigo-200/60 mb-4">
        <span class="flex h-2 w-2 rounded-full bg-indigo-600"></span>
        <p class="text-xs font-semibold uppercase tracking-widest text-indigo-700">Dashboard</p>
      </div>
      
      <div class="flex flex-col gap-8 md:flex-row md:items-center md:justify-between">
        <div class="flex-1 flex items-center gap-6">
          <div>
            <%= link_to edit_user_registration_path, class: "hover:opacity-80 transition-opacity" do %>
              <% if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar, class: "w-20 h-20 rounded-full object-cover border-4 border-indigo-200 shadow-lg cursor-pointer hover:shadow-xl transition-shadow" %>
              <% else %>
                <div class="w-20 h-20 rounded-full bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-white text-3xl font-bold border-4 border-indigo-200 shadow-lg cursor-pointer hover:shadow-xl transition-shadow">
                  <%= current_user.email[0].upcase %>
                </div>
              <% end %>
            <% end %>
          </div>
          <div>
            <h1 class="text-4xl md:text-5xl font-bold tracking-tight text-slate-900 mb-2">
              Bem-vindo, <span class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 bg-clip-text text-transparent"><%= (@user&.username.presence || current_user.username.presence || current_user.email.split('@').first) %></span>
            </h1>
            <p class="text-lg text-slate-600 max-w-2xl">
              Sua plataforma de gestão de provas e avaliações
            </p>
          </div>
        </div>

        <div class="flex flex-wrap gap-3">
          <%= link_to "✨ Nova prova", new_prova_path, class: "inline-flex items-center justify-center gap-2 rounded-lg bg-gradient-to-r from-indigo-600 to-blue-600 hover:from-indigo-700 hover:to-blue-700 px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-indigo-500/30 transition-all hover:shadow-xl hover:-translate-y-0.5" %>
          <%= link_to "📋 Minhas provas", provas_path, class: "inline-flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white hover:bg-slate-50 px-6 py-3 text-sm font-semibold text-slate-900 shadow-sm transition-all" %>
          <%= link_to "👥 Meus alunos", alunos_path, class: "inline-flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white hover:bg-slate-50 px-6 py-3 text-sm font-semibold text-slate-900 shadow-sm transition-all" %>
        </div>
      </div>
    </div>

    <!-- Stats Cards -->
    <div class="max-w-7xl mx-auto mb-12">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- Provas Card -->
        <div class="group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm border border-slate-200/60 hover:shadow-lg transition-all hover:-translate-y-1">
          <div class="absolute inset-0 bg-gradient-to-br from-indigo-50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
          <div class="relative">
            <div class="flex items-center justify-between mb-4">
              <p class="text-xs font-semibold uppercase tracking-wider text-slate-500">Provas Criadas</p>
              <div class="p-3 rounded-lg bg-indigo-100 text-indigo-600">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
              </div>
            </div>
            <p class="text-3xl font-bold text-slate-900"><%= current_user.provas.count %></p>
            <p class="text-sm text-slate-600 mt-2">Prova<%= current_user.provas.count != 1 ? 's' : '' %> ativa<%= current_user.provas.count != 1 ? 's' : '' %></p>
          </div>
        </div>

        <!-- Questões Card -->
        <div class="group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm border border-slate-200/60 hover:shadow-lg transition-all hover:-translate-y-1">
          <div class="absolute inset-0 bg-gradient-to-br from-emerald-50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
          <div class="relative">
            <div class="flex items-center justify-between mb-4">
              <p class="text-xs font-semibold uppercase tracking-wider text-slate-500">Questões</p>
              <div class="p-3 rounded-lg bg-emerald-100 text-emerald-600">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
            </div>
            <p class="text-3xl font-bold text-slate-900"><%= current_user.provas.sum { |p| p.questoes.count } %></p>
            <p class="text-sm text-slate-600 mt-2">Questões criadas</p>
          </div>
        </div>

        <!-- Alunos Card -->
        <div class="group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm border border-slate-200/60 hover:shadow-lg transition-all hover:-translate-y-1">
          <div class="absolute inset-0 bg-gradient-to-br from-purple-50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
          <div class="relative">
            <div class="flex items-center justify-between mb-4">
              <p class="text-xs font-semibold uppercase tracking-wider text-slate-500">Alunos</p>
              <div class="p-3 rounded-lg bg-purple-100 text-purple-600">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-2a6 6 0 0112 0v2zm0 0h6v-2a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
              </div>
            </div>
            <p class="text-3xl font-bold text-slate-900"><%= current_user.alunos.count %></p>
            <p class="text-sm text-slate-600 mt-2">Aluno<%= current_user.alunos.count != 1 ? 's cadastrad' : ' cadastrad' %><%= current_user.alunos.count != 1 ? 'os' : 'o' %></p>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Actions Section -->
    <div class="max-w-7xl mx-auto mb-12">
      <div class="rounded-2xl bg-gradient-to-br from-indigo-600 to-blue-600 p-8 shadow-lg relative overflow-hidden">
        <div class="absolute inset-0 opacity-10">
          <div class="absolute top-0 right-0 w-64 h-64 bg-white rounded-full -mr-32 -mt-32"></div>
        </div>
        <div class="relative">
          <h2 class="text-2xl font-bold text-white mb-6">Ações rápidas</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <button class="group flex items-center gap-3 p-4 rounded-lg bg-white/10 hover:bg-white/20 transition-all text-white font-semibold">
              <div class="p-2 rounded-lg bg-white/20 group-hover:bg-white/30 transition-all">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
              </div>
              <span><%= link_to "Criar nova prova", new_prova_path, class: "text-white hover:text-white" %></span>
            </button>
            <button class="group flex items-center gap-3 p-4 rounded-lg bg-white/10 hover:bg-white/20 transition-all text-white font-semibold">
              <div class="p-2 rounded-lg bg-white/20 group-hover:bg-white/30 transition-all">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-2a6 6 0 0112 0v2zm0 0h6v-2a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
              </div>
              <span><%= link_to "Importar alunos via CSV", import_alunos_path, class: "text-white hover:text-white" %></span>
            </button>
            <button class="group flex items-center gap-3 p-4 rounded-lg bg-white/10 hover:bg-white/20 transition-all text-white font-semibold">
              <div class="p-2 rounded-lg bg-white/20 group-hover:bg-white/30 transition-all">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
                </svg>
              </div>
              <span><%= link_to "Importar provas via CSV", import_provas_path, class: "text-white hover:text-white" %></span>
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Onboarding Steps -->
    <div class="max-w-7xl mx-auto">
      <div class="mb-6">
        <h2 class="text-2xl font-bold text-slate-900">Primeiros passos</h2>
        <p class="text-slate-600 mt-1">Siga este guia para preparar sua plataforma</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- Step 1 -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-r from-indigo-600 to-purple-600 rounded-2xl opacity-0 group-hover:opacity-10 transition-opacity"></div>
          <div class="relative rounded-2xl border-2 border-dashed border-slate-300 bg-white p-8 group-hover:border-indigo-400 transition-colors">
            <div class="flex items-center justify-center h-12 w-12 rounded-full bg-gradient-to-br from-indigo-600 to-purple-600 text-white font-bold text-lg mb-4">
              1
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Crie uma prova</h3>
            <p class="text-slate-600 text-sm mb-4">
              Comece definindo o nome da prova, a disciplina, data e adicione suas questões.
            </p>
            <%= link_to "Criar prova →", new_prova_path, class: "text-sm font-semibold text-indigo-600 hover:text-indigo-700" %>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-r from-emerald-600 to-teal-600 rounded-2xl opacity-0 group-hover:opacity-10 transition-opacity"></div>
          <div class="relative rounded-2xl border-2 border-dashed border-slate-300 bg-white p-8 group-hover:border-emerald-400 transition-colors">
            <div class="flex items-center justify-center h-12 w-12 rounded-full bg-gradient-to-br from-emerald-600 to-teal-600 text-white font-bold text-lg mb-4">
              2
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Configure questões</h3>
            <p class="text-slate-600 text-sm mb-4">
              Adicione questões de múltipla escolha ou dissertativas com seus respectivos gabaritos.
            </p>
            <%= link_to "Ver provas →", provas_path, class: "text-sm font-semibold text-emerald-600 hover:text-emerald-700" %>
          </div>
        </div>

        <!-- Step 3 -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-pink-600 rounded-2xl opacity-0 group-hover:opacity-10 transition-opacity"></div>
          <div class="relative rounded-2xl border-2 border-dashed border-slate-300 bg-white p-8 group-hover:border-violet-400 transition-colors">
            <div class="flex items-center justify-center h-12 w-12 rounded-full bg-gradient-to-br from-violet-600 to-pink-600 text-white font-bold text-lg mb-4">
              3
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Corrija e avalie</h3>
            <p class="text-slate-600 text-sm mb-4">
              Use IA para corrigir dissertativas e gere relatórios com análise de desempenho.
            </p>
            <%= link_to "Explorar recursos →", provas_path, class: "text-sm font-semibold text-violet-600 hover:text-violet-700" %>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer Actions -->
    <div class="max-w-7xl mx-auto mt-16 pt-8 border-t border-slate-200">
      <div class="flex flex-col md:flex-row items-center justify-between gap-6">
        <div class="flex items-center gap-4">
          <% if current_user.avatar.attached? %>
            <%= image_tag current_user.avatar, class: "w-12 h-12 rounded-full object-cover border-2 border-indigo-200" %>
          <% else %>
            <div class="w-12 h-12 rounded-full bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-white font-bold border-2 border-indigo-200">
              <%= current_user.email[0].upcase %>
            </div>
          <% end %>
          <div>
            <p class="text-sm font-semibold text-slate-900"><%= current_user.username.presence || current_user.email.split('@').first %></p>
            <p class="text-xs text-slate-600"><%= current_user.email %></p>
          </div>
        </div>
        <div class="flex gap-2">
          <%= link_to "⚙️ Meu perfil", edit_user_registration_path, class: "inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-indigo-600 transition-colors" %>
          <%= link_to "🚪 Sair", destroy_user_session_path, data: { turbo_method: :delete }, class: "inline-flex items-center gap-2 text-sm font-semibold text-rose-600 hover:text-rose-700 transition-colors" %>
        </div>
      </div>
    </div>
  </div>
</div>
